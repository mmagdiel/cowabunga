import React from "react"
import { Commands } from "~tracking"
import { Layout } from "../../core"

function commands() {
  return (
    <Layout>
      <h1>Commands</h1>
      <Commands />
    </Layout>
  )
}

export default commands
